import rhinoscriptsyntax as rs

def dividePointsInX():
    points = rs.GetPoints()
    x = 0
    for point in points:
        arCoord = rs.GetPointCoordinates();
        
        
def sortPoints(points):
    sortedPoints = 